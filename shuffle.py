from random import shuffle
card=["Queen","jack","King","Hearts","Spade"]
shuffle(card)
for cards in card:
    print(cards)
    
# what this function called shuffle does is it takes the input as a list and shuffles it 
# it does not return a value what it does is it shuffles the entire list